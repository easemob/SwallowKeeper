http {
    # read upstreams generated by script scripts/update_nginx_script which will sync consul service servers into 
    # conf/dyups.consul.upstream.conf
    dyups_upstream_conf  conf/dyups.consul.upstream.conf;
    include conf/*.conf;
 
}
